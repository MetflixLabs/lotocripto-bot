export enum LotocriptoEndpointEnum {
  USERS = '/users',
  USER_STATE = '/userState',
  PARTICIPANTS = '/participants',
  PARTICIPANTS_LENGTH = '/participants/length',
  PARTICIPANTS_ALLSOCKETS = '/participants/allSockets',
  LOGIN = '/login',
  WINNERS = '/winners',
  ELEGIBLE = '/elegible'
}
